@using FictionalLanguageTranslator.Models.Application.Service;
@using FictionalLanguageTranslator.Models.Application.Value;
@model GermanStyleModel
@{
    ViewData["Title"] = "German Style";
}

<div class="jumbotron">
    <h1>ドイツ語っぽい発音の架空言語翻訳機</h1>
</div>

<div class="container">
    <div class="row">
        <div class="col-sm">
            <form asp-action="Index" method="post" class="form-group">
                <div class="card">
                    <div class="card-header">Japanese</div>
                    <div class="card-body">
                        <textarea asp-for="japanese" cols="120" rows="6" class="form-control"></textarea>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Translate</button>
            </form>
        </div>
        @if(@Model.fictional.IsNotEmpty())
        {
            <div class="col-sm">
                <div class="card">
                    <div class="card-header">Fictional</div>
                    <pre class="card-body">@Model.fictional</pre>
                </div>
            </div>
        }
        @if(@Model.pronunciation.IsNotEmpty())
        {
            <div class="col-sm">
                <div class="card">
                    <div class="card-header">Pronunciation</div>
                    <pre class="card-body">@Model.pronunciation</pre>
                </div>
            </div>
        }
    </div>
</div>
